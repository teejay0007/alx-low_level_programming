#include <stdio.h>
/**
 * main - lowercase and uppercase prints
 *
 * Description: this program prints lower case and upper case
 *
 * Return: 0
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	for (c = 'A'; c <= 'Z'; c++)
		putchar(c);
	putchar('\n');
	return (0);
}
