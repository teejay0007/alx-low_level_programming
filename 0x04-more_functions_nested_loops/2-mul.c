#include "main.h"
/**
 * mul - returns the product of two integers
 *
 * @a: passes in first int
 * @b: passes in second int
 *
 * Return: product of ints
 */
int mul(int a, int b)
{
	return (a * b);
}
